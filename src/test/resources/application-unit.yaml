spring:
  jpa:
    defer-datasource-initialization: 'true'
    hibernate:
      format_sql: 'true'
      ddl-auto: create
    show-sql: 'true'
application:
  security:
    expiration: '84600000'
    secretKey: ${SECRET_KEY}
    refreshToken:
      expiration: '604800000'